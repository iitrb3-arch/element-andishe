name: Validate Git LFS

on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    name: Validate
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: 'true'

      - run: |
          ./tools/validate_lfs.sh
